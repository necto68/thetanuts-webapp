import { HeaderContainer, HeaderButtons } from "../../header/components";

import { Metrics } from "./Metrics";

export const Header = () => (
  <HeaderContainer>
    <Metrics />
    <HeaderButtons />
  </HeaderContainer>
);
