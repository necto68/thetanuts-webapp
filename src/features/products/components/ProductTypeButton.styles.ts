import styled from "styled-components";

import { BaseButton } from "../../shared/components";

export const TypeButton = styled(BaseButton)`
  border: 0;
  border-radius: 30px;
`;

export const Title = styled.span`
  font-family: Roboto;
  font-weight: 400;
  font-size: 21px;
`;
